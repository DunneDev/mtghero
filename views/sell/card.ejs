<% layout('layouts/boilerplate') %>

<h1><%= card.name %></h1>
<div>
  <img src="<%= card.images.small %>" alt="<%= card.name %>" />
</div>
<form action="/sell/<%= card.id %>" method="POST">
  <button>Sell</button>
</form>